[package]
name = "claw-fs"
version = "0.1.0"
edition = "2021"
description = "A safe, robust CLI tool for AI agents to manipulate text and code."

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
